# <slug>

## 用户需求
<原话或清洗后，≤ 3 行>

## 数据形状
<按来源分块：涉及的来源各起 `### API` / `### Form` / `### URL` / `### Storage` / `### Persistence` 小节；前 4 类用代码块写应用层类型；`### Persistence`（涉及持久化层时出现）按项目 ORM/DDL 语法写：字段类型 + 索引（含复合索引顺序）+ 关系/级联 + 唯一/非空约束 + migration 顺序与回填策略。不涉及的来源直接删小节>

## 状态边界
<按 5 类（server / client / URL / form / cache）涉及的写，列表式：状态名 + 转移条件。**这里的 server 指"应用层服务端运行时状态"（如 session / 内存缓存 / 请求作用域上下文），不写"哪个进程承载哪类服务"——后者写在「运行拓扑」节**>
> 未涉及：<5 类中未涉及者，全涉及写"无">

## 接口契约
<分同步和异步两块条件出现>
### 同步
<METHOD /path → 状态码 形状，列表式；每条注明范式（REST/GraphQL/RSC/RPC/SSE/WS）+ 鉴权位置（header/cookie/query）+ 错误形状>

### 异步
<消息名 → payload 形状，列表式；每条注明通信介质（队列/流/pub-sub）+ 幂等键 + 重试策略（次数/退避）+ 超时 + 死信路径>

## 运行拓扑
<条件节：仅多服务/多进程/容器化项目出现，单服务项目整节删掉>
- 进程边界：<哪些是 web server / 独立 worker / 外部服务>
- 通信路径：<端到端调用链一行画清>
- 部署单元：<容器/服务清单 + 依赖顺序 + 健康检查 + 优雅关闭>
- 跨语言边界：<如有，在哪一层切、契约怎么固化；无则省略>

## 验收点
<两类各按需写 `- [ ] ...`：业务功能按实际拆条；web 通用态分 3 类（四态 / a11y / 分页）每类涉及就单独 1 条。两类合计 ≤ 10 条，通用态 ≤ 3 条。纯重构无业务功能时只写通用态，反之亦然>
> 未涉及：<3 类中未涉及者，全涉及写"无"。此 `>` 行不计入 10 条上限>

## 参照点
- <path:line> <一句>，path 用 `/` 开头表项目根相对（如 `/src/api/auth.<ext>:5`），禁未 Read 过的；无则"无"；优先看 routes / loaders / api handlers / middleware / 类型定义；涉及持久化必看 schema 定义文件 + migration 历史；涉及多服务必看容器编排配置 + 各服务启动脚本 + 队列/RPC 客户端初始化

## 假设（3 类前缀；选型=我选了什么；风险=选完仍存的不确定）
- 推断：<用户没明说的技术细节，如"DOM 用 data-theme"、"防抖 300ms"，每条一行>
- 选型：<多选项中你的选择+理由。通用知识理由如"cursor 而非 offset，列表插入 offset 会丢/重"任何时候可给；项目实证理由如"复用 lockfile/manifest 里已有的 X"——**所引文件必须本轮已 Read，禁凭印象**。无技术取舍省略此前缀>
- 风险：<代码/需求里有线索的风险点，如"q 快速变化竞态"、"EventSource 不支持 header"、"加索引锁表"、"消息重复消费"、"任务丢失"，无则省略此前缀>
