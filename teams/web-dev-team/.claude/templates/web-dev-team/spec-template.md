# <slug>

## 用户需求
<原话或清洗后，≤ 3 行>

## 数据形状
<按来源分块：涉及的来源各起 `### API` / `### Form` / `### URL` / `### Storage` 小节，块内用代码块写类型；不涉及的来源直接删掉小节>

## 状态边界
<按 5 类（server / client / URL / form / cache）涉及的写，列表式：状态名 + 转移条件>
> 未涉及：<5 类中未涉及者，全涉及写"无">

## 接口契约
<METHOD /path → 状态码 形状，列表式；每条注明范式（REST/GraphQL/RSC/SSE/WS）+ 鉴权位置（header/cookie/query）+ 错误形状>

## 验收点
<两类各按需写 `- [ ] ...`：业务功能按实际拆条；web 通用态分 3 类（四态 / a11y / 分页）每类涉及就单独 1 条。两类合计 ≤ 10 条，通用态 ≤ 3 条。纯重构无业务功能时只写通用态，反之亦然>
> 未涉及：<3 类中未涉及者，全涉及写"无"。此 `>` 行不计入 10 条上限>

## 参照点
- <path:line> <一句>，path 用 `/` 开头表项目根相对（如 `/src/api/auth.ts:5`），禁未 Read 过的；无则"无"；优先看 routes / loaders / api handlers / middleware / 类型定义

## 假设（3 类前缀；选型=我选了什么；风险=选完仍存的不确定）
- 推断：<用户没明说的技术细节，如"DOM 用 data-theme"、"防抖 300ms"，每条一行>
- 选型：<多选项中你的选择+理由。通用知识理由如"cursor 而非 offset，列表插入 offset 会丢/重"任何时候可给；项目实证理由如"用 React Query 因 package.json 已有 @tanstack"——**所引文件必须本轮已 Read，禁凭印象**。无技术取舍省略此前缀>
- 风险：<代码/需求里有线索的风险点，如"q 快速变化竞态"、"EventSource 不支持 header"，无则省略此前缀>
